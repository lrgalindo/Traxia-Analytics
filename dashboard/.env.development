# Dashboard — development
# The Vite dev server already proxies /v1 and /health to localhost:8000
# (see vite.config.ts > server.proxy), so VITE_API_URL is intentionally empty.
# Start the backend first: source secrets/dev.env.local && uvicorn cloud.main:app --port 8000
VITE_API_URL=
